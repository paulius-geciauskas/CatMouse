package com.paulius.comp.games.catmouse;

import com.paulius.comp.games.catmouse.model.Coordinate;
import com.paulius.comp.games.catmouse.model.MapPoint;
import com.paulius.comp.games.catmouse.model.PointType;

/**
 * Created by Admin on 2018-12-06.
 */

public class Mouse {
 /*   private int x;
    private int y;

    public MapPoint[][] moveRight(MapPoint[][] currentMap) {
      //  com.paulius.comp.games.catmouse.model.MapPoint[][]  map = currentMap;
        if (currentMap[mouseCoordinates(currentMap).getX()][mouseCoordinates(currentMap).getY()+1].toString().equals("E")) {
         //   currentMap[mouseCoordinates(currentMap).getX()][mouseCoordinates(currentMap).getY()].setP(PointType.E);
            currentMap[mouseCoordinates(currentMap).getX()][mouseCoordinates(currentMap).getY()+1].setP(PointType.MOUSE);
        }
//  com.paulius.comp.games.catmouse.model.Coordinate mouse = new com.paulius.comp.games.catmouse.model.Coordinate();
//   mouseCoordinates(currentMap).getX()
        return currentMap;
    }



 */

}
